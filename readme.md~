git pushing
	
git init
git add .
git commit -m "First commit"


// the below are optional only required at first time
git remote add origin remote repository URL
# Sets the new remote
git remote -v
# Verifies the new remote URL
git push origin master
